# Python Install

- **Package**: `agent0-sdk` (PyPI)
- **Version**: 1.5.3 (at time of writing)
- **Runtime**: Python 3.8+

```bash
pip install agent0-sdk
```

Main import:

```python
from agent0_sdk import SDK
```

> Version-sensitive: package name `agent0-sdk` and import `agent0_sdk.SDK` reflect v1.5.3.

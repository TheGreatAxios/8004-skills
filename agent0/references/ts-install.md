# TypeScript Install

- **Package**: `agent0-sdk` (npm)
- **Version**: 1.5.3 (at time of writing)
- **Runtime**: Node.js 22+
- **Module format**: ESM only — use `import`, not `require`

```bash
npm install agent0-sdk
```

Main export:

```ts
import { SDK } from 'agent0-sdk';
```

> Version-sensitive: package name `agent0-sdk` and export `SDK` reflect v1.5.3.
